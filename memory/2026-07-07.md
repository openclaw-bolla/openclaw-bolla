# 2026-07-07 (Dienstag)

## AURORA-Signatur-Essay: LinkedIn live, Medium verschoben
- Essay als LinkedIn-Artikel veröffentlicht (Titelbild eigene Komposition aus AURORA_Cover.jpg, links KI-Gesicht/AURORA, rechts Marlie am Monitor). Kopfzeile korrigiert: „Teacher & Former IT Executive — Wrote an AI Thriller (with an AI)" statt generischem „Pensioner".
- Fehler gefunden+gefixt: deutscher Books2Read-UBL in `project_aurora_identifiers.md` war kaputt (404, nie verifiziert) → ersetzt durch direkte Amazon-Links.
- Medium-Posting verschoben (Chris will erst anderes Projekt), Text liegt bereit. EN-Website+Sprachschalter für chrismandel.de als eigenes Backlog-Projekt festgehalten (EN-Texte schon fertig übersetzt). Board aktualisiert.

## Reiseplaner: Word-Export neu gebaut + Freiburg-Tage restrukturiert
- Server-seitiger echter `.docx`-Export (`reise_sightseeing_docx()` in `mission_control_api.py`): Seite 1 Übersichtskarte, je Reisetag eine Seite mit Halbtag-Zuordnung, Anfahrt, Beschreibung, Tipp, Highlights.
- Chris' Kritik an den Freiburg-Tagen (3 Punkte: Aufteilung auf Halbtage inkl. Kaiserstuhl fehlt, Freibad-Idee statt Feldberg-Verlängerung, „Freier Tag"-Wiederholung) behoben: neues Datenfeld `tagesplan` (Format `Tag|Halbtag|Titel|Text`) mit 4 halbtags-genauen Einträgen — Tag 4 NM Ankunft & Lorettobad (recherchiert: ältestes Familienfreibad Deutschlands, 1842), Tag 5 NM Altstadt/Münster/Bächle, Tag 6 VM Schlossberg, Tag 6 NM Markthalle & Münstermarkt. Backend-Helper `_reise_tagesplan_items()` + `_add_tagesplan_block()`.
- Getestet (python-docx-Auszug bestätigt korrekte Struktur), Vorschau auf Desktop. **Noch nicht committet** — wartet auf Chris' Freigabe der gesamten Reiseplaner-Änderungen (auch ältere aus 06.07: Tageszeit-Feld, Tagesübersicht-Leiste-Entscheidung).

## Memory-Ordner: Desktop aufgeräumt (Chris)
- Chris hat alle AURORA-Marketingdateien vom Desktop in `Dokumente\Bolla\AURORA\6_Marketing\` einsortiert → gemerkt in [[reference_aurora_marketing_ordner]], nicht mehr auf Desktop suchen.

## Memory-Hygiene-Durchgang (Nachmittag)
- `_memhealth.md` meldete ACTION NEEDED (aktuell.md 49 KB, MEMORY.md-Index 26,6 KB, 205 Dateien gesamt 454 KB, 19 verwaiste Memories).
- aktuell.md auf aktuelle offene Fäden eingedampft — Verlauf 04.–06.07. lebt in den Tagesnotizen (`2026-07-04.md` bis `2026-07-06.md`) + `data/board.json` (AURORA/Immobilien/Schuljahr-Projekte mit Aktionsstatus) + den themenspezifischen Memory-Dateien weiter, nichts verloren.

## Schuljahr 26/27: MP-Seite von Modulliste auf echte Terminplanung umgebaut (Nachmittag/Abend)
- Alten Seiteninhalt (Module/offene Fragen) auf Chris' Wunsch komplett ersetzt: `data/schuljahr2627.json` neu mit 156 Terminen (39 Mi + 39 Do × Slots 1./2. Std + 6./7. Std), Ferien aus SH-Kalender rausgerechnet (Start Mo 17.08.26, Ende Fr 02.07.27).
- Klasse↔Slot-Zuordnung ist noch offen (Stundenplan fehlt), aber **„I"/„II" = zwei Gruppen derselben Klasse im 4-Wochen-Rhythmus** (per Chris bestätigt) — als `gruppe`-Feld + Farb-Badge in der Tabelle sichtbar.
- Thema-Spalte jetzt live editierbar (`POST /api/schuljahr2627/update`), daneben 📌-Auftragsknopf (`POST /api/schuljahr2627/nudge`) baut z.B. „erstelle dazu ein Arbeitsblatt"-Prompts in die Bolla-Queue.
- Zwischendurch CSS-Falle gefunden+behoben: Boxen mit `rgba(255,255,255,0.03)` waren auf dem Foto-Hintergrund praktisch unsichtbar/transparent — auf `var(--card)` umgestellt (Chris: passiert mir öfter bei neuen Seiten, künftig gleich richtig machen).
- `mission_control_api.py` dafür neu gestartet. Reiseplaner-Änderungen (siehe oben) bleiben weiterhin unabhängig unkommittet.
- Details: [[project_schuljahr2627]], aktueller Stand in `aktuell.md`.
