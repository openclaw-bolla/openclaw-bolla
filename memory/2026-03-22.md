# 2026-03-22 - Tagesnotizen

## Token Watcher Setup

### Problem
- `token_watcher.py` lief nicht mehr (Prozess war gestorben)
- Bug: `$orderby` in Graph API URL warf `InefficientFilter`-Fehler → wurde gefixt (orderby entfernt)
- Watcher hatte keinen Autostart → nach Neustart/Session-Ende war er weg

### Lösung
- Bug in `find_token_emails()` gefixt (kein `$orderby` mehr)
- Check-Intervall von 8h auf 1h reduziert
- VBS-Script erstellt: `scripts/start_token_watcher.vbs`
- VBS im Windows-Autostart hinterlegt: `C:\Users\ernst\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\`
- Watcher läuft jetzt mit nohup, PID 495

### Wichtige Regel (von Chris)
- **KEINEN Token manuell in auth-profiles.json eintragen!**
- Der Watcher darf Token eintragen (das ist sein Job), aber ich nicht manuell
- Beim letzten Versuch hatte ich einen ungültigen Token eingetragen

### Ablauf (wie besprochen)
1. Robin schickt neuen `sk-ant-...` Token per Mail (robinmandel@outlook.de → ernstmandel@outlook.de)
2. Watcher prüft stündlich, extrahiert Token, trägt ihn in auth-profiles.json ein
3. Gateway wird neu gestartet, Mail wird gelöscht
4. Nach PC-Neustart: Autostart feuert VBS → startet Watcher automatisch

### Token-Ablauf heute Nacht
- Aktueller Token läuft ca. 07:00 Uhr ab
- Robin schickt neuen Token per Mail ca. 07:13
- Wenn Chris den PC erst um 10:00 startet → Watcher greift sofort beim Start und trägt neuen Token ein

## Erkenntnisse
- `nohup` überlebt geschlossene PowerShell und Sleep-Modus ✅
- Refresh Token für Microsoft/Outlook läuft automatisch, kein manuelles Login nötig
- Watcher kann nicht prüfen ob Token gültig ist → Vertrauen auf Robin

## Dateien
- Script: `/home/bolla/.openclaw/workspace/scripts/token_watcher.py`
- Autostart: `/home/bolla/.openclaw/workspace/scripts/start_token_watcher.vbs`
- Log: `/home/bolla/.openclaw/workspace/logs/token_watcher.log`
- State: `/home/bolla/.openclaw/workspace/config/token_watcher_state.json`
