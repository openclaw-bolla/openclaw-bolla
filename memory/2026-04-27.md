# 2026-04-27

## Mission Control — E-Mail Multi-Select & UI-Fixes

### Multi-Select für Mails
- `_selectedMail` (singular) → `_selectedMails[]` (Array) umgestellt
- Klick auf Mail-Row togglet Auswahl, nochmal klicken hebt sie auf
- Mehrere Mails gleichzeitig auswählbar — cmd-bar zeigt „📧 N Mails"
- ✕-Button in cmd-bar hebt gesamte Auswahl auf (`clearMailSelection()`)
- Ausgewählte Mails: lila Hintergrund + Rahmen (`.mail-row.selected`)

### Backend: mail_command() auf Arrays erweitert
- Akzeptiert jetzt `mails: [...]` (Array) statt nur `mail: {}`
- Löschen: loopt durch alle, löscht Outlook + wtnet in einem Aufruf, Zusammenfassung
- Analyse/Prüfen: übergibt alle Mails als Kontext an Claude
- Antworten: nur bei Einzelauswahl, sonst Fehlermeldung

### Ergebnis-Panel verschmälert
- War: `left:0; right:0` → volle Bildschirmbreite, unschön
- Jetzt: zentriertes schwebendes Card, `width: min(640px, calc(100vw - 40px))`
- Schwebt 68px über cmd-bar, slide-up mit fade-in
- `border-top` Farbe wechselt je nach Ergebnistyp (grün/orange/rot/lila)

### Cmd-Bar eingerückt
- `left:200px` statt `left:0` → bündig mit Hauptinhalt, nicht unter Sidebar

## Projekte-Seite in Mission Control (Neubau)

### Projektkarte Immobilienkauf Bayern
- Status-Badge "IN BEARBEITUNG", Timeline-Balken (Jetzt → Herbst 2030)
- Tags: Bayern, 3 Jahre, Eigennutzung — Nächste Schritte als Checkliste
- Chris & Reni planen ETW im Raum München West, ~50km rund um Herrsching, ohne München direkt

### Makler-Tracker (32 Makler)
- Regionen: Ammersee, Starnberg, Landsberg, Weilheim, FFB, Gilching
- Statusfilter: Alle / Neu / E-Mail gesendet / Feedback / Termin
- Per Klick Status cyclen, Notizen hinterlegen
- Backend: `/api/makler` mit persistentem JSON, Makler-Liste aus WebSearch/WebFetch zusammengestellt

### Immo-News Bayern (rechte Spalte)
- Aktuelle Markt-News, gecacht, Klick → Bolla-Zusammenfassung
- Backend: `/api/immo-news` + `/api/immo-news/refresh`

## Newsletter-Scanner

- `scripts/newsletter_scanner.py` — scannt Outlook + wtnet-Mails auf Newsletter
- Watchlist-Begriffe konfigurierbar, Ergebnisse gecacht in `cache/newsletter_results.json`
- Sofortsuche: Begriff eingeben → direkt in aktuellen Newslettern suchen
- MC-Panel in der Mail-Seite eingebettet
- Backend-Routen: `/api/newsletter`, `/api/newsletter/watchlist`, `/api/newsletter/scan`, `/api/newsletter/search`

## Clipboard-Seite

- Eigene Nav-Seite in MC (📋 Clipboard)
- Textarea → Speichern/Kopieren/Löschen, Timestamp
- Backend: `GET/POST /api/clipboard` → `config/clipboard.json`
- Zweck: gemeinsamer Zettel zwischen WSL, Windows, Browser

## Settings-Änderung
- `WebSearch` + `WebFetch` dauerhaft in `~/.claude/settings.json` Allow-Liste eingetragen
- Hintergrund: Chris ist oft in der MC-UI und kann dort keine Permission-Prompts freigeben

## Immo-Bookmarks (Gespeicherte Tipps)

- 📌 Speichern-Button in Bolla-Zusammenfassungs-Panel (erscheint nach Laden der Summary)
- Kategorie-Select: Ausflüge / Radwege / Restaurants / Regionen / Sonstiges
- Box direkt unter den Immo-News in der rechten Spalte, blauer Header
- Jeder Eintrag: content-basiertes Icon aus Titel-Keywords (🚲⛵🍺🏰🌲🥾⛷️🎪…)
- Notizfeld pro Eintrag, Zusammenfassung ausklappbar, Löschen-Button
- Filter nach Kategorie, persistent in `config/immo_bookmarks.json`
- Backend: `GET/POST /api/immo-bookmarks`, `/api/immo-bookmarks/delete`, `/api/immo-bookmarks/update`

## Kriterien-Box (Immobilienkauf)

- Handschrift-Scans ausgelesen: `IMG_20260427_182024.jpg` + `IMG_20260427_182045.jpg`
- Kriterien gruppiert in Lage / Objekt / Optional (Preise weggelassen)
- Box in linker Spalte unter Projektkarte, lila/indigo Header
- Drag & Drop zum Priorisieren innerhalb und zwischen Gruppen
- Reihenfolge persistent in `config/immo_criteria.json`
- Backend: `GET/POST /api/immo-criteria`

## Offen
- Telegram-Desktop-Icon auf dem Surface Book (wird in separater Session auf dem Book geklärt)
