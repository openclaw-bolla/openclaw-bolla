# 2026-04-27

## Mission Control — E-Mail Multi-Select & UI-Fixes

### Multi-Select für Mails
- `_selectedMail` (singular) → `_selectedMails[]` (Array) umgestellt
- Klick auf Mail-Row togglet Auswahl, nochmal klicken hebt sie auf
- Mehrere Mails gleichzeitig auswählbar — cmd-bar zeigt „📧 N Mails"
- ✕-Button in cmd-bar hebt gesamte Auswahl auf (`clearMailSelection()`)
- Ausgewählte Mails: lila Hintergrund + Rahmen (`.mail-row.selected`)

### Backend: mail_command() auf Arrays erweitert
- Akzeptiert jetzt `mails: [...]` (Array) statt nur `mail: {}`
- Löschen: loopt durch alle, löscht Outlook + wtnet in einem Aufruf, Zusammenfassung
- Analyse/Prüfen: übergibt alle Mails als Kontext an Claude
- Antworten: nur bei Einzelauswahl, sonst Fehlermeldung

### Ergebnis-Panel verschmälert
- War: `left:0; right:0` → volle Bildschirmbreite, unschön
- Jetzt: zentriertes schwebendes Card, `width: min(640px, calc(100vw - 40px))`
- Schwebt 68px über cmd-bar, slide-up mit fade-in
- `border-top` Farbe wechselt je nach Ergebnistyp (grün/orange/rot/lila)

### Cmd-Bar eingerückt
- `left:200px` statt `left:0` → bündig mit Hauptinhalt, nicht unter Sidebar
