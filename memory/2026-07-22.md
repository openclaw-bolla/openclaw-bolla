---
name: 2026-07-22
description: Tagesnotiz — AURORA-Kontakte (Efiloğlu, Ammanath), Handouts-Ordner + Kurstag 1 aufgeräumt, itslearning-Anstups-Workflow gebaut/getestet/bereinigt
metadata:
  node_type: memory
  type: project
---

# 22.07.2026

## AURORA Runde 2 — zwei weitere Kontakte raus

- **LinkedIn-Kontakt-UI dokumentiert:** "Nachricht" = Premium-Falle, "Mehr" → "Vernetzen" = kostenlos. Details: [[reference_linkedin_kontakt_ui]].
- **Yasemin Efiloğlu** per E-Mail kontaktiert (echte Adresse aus LinkedIn-Info-Bereich gefunden, nicht über die Plattform selbst).
- **Beena Ammanath** (Deloitte AI Institute) kontaktiert — einzige der 8 nachrecherchierten 🟡/🔴-Kontakte mit verwertbarer eigener Mailadresse.
- **Fehler passiert und korrigiert:** Erster Efiloğlu-Entwurf behauptete fälschlich, ihren Podcast gehört zu haben — reine Erfindung. [[feedback_keine_erfindungen]] entsprechend geschärft: gilt jetzt auch für behauptete eigene Handlungen in Outreach-Texten, nicht nur biografische Fakten. Sarah-Engels-Entwurf hat denselben Fehler und muss vor Versand noch korrigiert werden.
- Google-Drive-MCP-Server verbunden (Nice-to-have, kein Sicherheitsrisiko).

## Handouts-Ordner aufgeräumt + Kurstag 1 (Grundbegriffe I) inhaltlich fertiggestellt

- **Datei-Chaos-Ursache gefunden:** Windows sortiert alphabetisch, nicht numerisch — einstellige Tagespräfixe (`1-`, `2-`…) rissen die Reihenfolge auseinander. Alle 14 Bestandsdateien auf zweistellig umbenannt (`00-`…`09-`), Konvention dauerhaft in [[project_schuljahr2627]] festgehalten.
- Alte Dateinamen-Referenzen in zwei PPTX (Tag 0/1) korrigiert.
- Inhaltlicher Konsistenz-Check Folien vs. Praktikum: "Umbenennen" fehlte als Übung — ohne neuen Schritt in die Praktikums-HTML integriert (Timing bei ~35 Min war schon knapp).

## itslearning-Anstups-Workflow: gebaut, live getestet, korrigiert, bereinigt, konsolidiert

Der größte Brocken des Abends — kompletter Lebenszyklus einmal durchgespielt:

1. **Erster echter Live-Post:** PDF + Praktikum-HTML in alle 4 „I"-Kurse (7a/7b/7c/7d I) hochgeladen und individuell formulierte Mitteilungen veröffentlicht. Technisch zäh (itslearnings Custom-Uploader: verschachtelte iFrames, Klick-Popover statt Datei-Dialog, Dateiname im DOM in zwei Spans gesplittet, ein Confirm-Button der `<input>` statt `<button>` ist) — alle Fallstricke gelöst und dokumentiert.
2. **Chris' Review fand 2 echte Fehler:** Mitteilungstext sagte fälschlich "vorher auf den Stick laden" (Download passiert tatsächlich erst am Kurstag selbst), und die per LibreOffice erzeugte PDF sah schlecht aus.
3. **Alles zurückgerollt** auf Chris' Wunsch: 4 Mitteilungen + 8 Ressourcen wieder gelöscht, verifiziert (Kurs zurück beim Ausgangszustand).
4. **PDF-Methode gefixt:** PowerPoint-COM-Automatisierung (`$pres.SaveAs($pdfPath, 32)`) statt LibreOffice — von Chris angeschaut und für gut befunden, ist jetzt Standardmethode.
5. **Alles für künftige Läufe konsolidiert:** generalisiertes Post-Skript (sammelt PDF+HTML-Standard-Paket automatisch per Tagesnummer), PDF-Export-Skript, zwei Notfall-Rückbau-Skripte — alle dauerhaft in `workspace/scripts/` abgelegt, Details in [[project_itslearning_automation]].
6. **Neue Dauerregel:** Bei MP-Anstups-Aufträgen nicht mehr vorab fragen/Entwurf zeigen, nur informieren dass gearbeitet wird — Korrektur danach statt Freigabe vorher (Ausnahme in [[feedback_website_schreibzugriff]]).

**Offen für nächstes Mal:** Soll Tag 1 zusätzlich eine Download-Anleitung/Live-Demo bekommen (Schüler haben das noch nie gemacht)? Mit Chris klären, bevor der nächste Post rausgeht.
