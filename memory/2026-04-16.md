# Session 16.04.2026

## Was gemacht wurde

### Cloudflare Tunnel / Mission Control
- **Error 1033** (Robin): Tunnel war kurz nach WSL-Standby down → Robin kam genau in die 2-Min-Lücke nach Neustart
- **cloudflared_healthcheck.sh** eingerichtet: alle 2 Min per Cron, prüft Prozess + HTTPS-Antwort
- Healthcheck verfeinert: erst beim **zweiten Fehler in Folge** Restart (Flag-Datei `/tmp/cloudflared_health_fail`) — verhindert unnötige Session-Unterbrechungen

### Mission Control Fixes
- **API_BASE-Hardcodierung** `http://127.0.0.1:18790` → `''` (same-origin) — Robin konnte vorher keine Daten laden
- **Modell-Anzeige** Opus → Sonnet (index.html Zeile 500 + tokenusage-Fallback)
- **ADB Verbindungs-Block**: IP und Port als getrennte Eingabefelder, IP vorausgefüllt mit `192.168.178.20`, localStorage speichert beide Werte separat
- JS-Fehlerbehandlung robuster gemacht (`showMsg()`-Helper statt direktem `out.innerHTML`)

### Spam-Watcher Fix
- Doppelte Log-Einträge behoben: `StreamHandler` aus `basicConfig` entfernt (Cron leitet stdout ohnehin in dieselbe Datei)

### Windows Energie-Einstellungen
- Bildschirm aus: 5 Min (AC), 3 Min (DC)
- Standby/Hibernate: **nie**
- Festplatte: 10 Min (AC)
- CPU max: 80% (AC)
- USB selective suspend + PCI Express ASPM: aktiviert
- Lockscreen-Timeout: 60s (verstecktes Setting `8EC4B3A5`)
- Alles per powercfg ohne Admin gesetzt

### Accounts PDF
- `/home/bolla/workspace/docs/accounts_overview.pdf` erstellt (reportlab)
- Kopie nach OneDrive: `D:\OneDrive\Dokumente\Bolla\accounts_overview.pdf`

### Memory
- `user_accounts.md`: Anthropic-Login ist **chrismandel13@gmail.com** (Änderung beim Support beantragt), Standard-Absender ernstmandel@outlook.de

## Skills-Diskussion
- **schedule**: für Chris' Use-Case ungeeignet (alles lokal, kein Cloud-Repo-Workflow)
- **simplify**: sinnvoll, noch nicht eingerichtet
- **loop**: bei Bedarf

## Offene Todos
- `simplify`-Skill einrichten
- Azure Speech Key rotieren (seit 12.04. fällig)
- IONOS SFTP-Passwort rotieren (seit 12.04. fällig)
- HKLM-Autostart (Nearby Share + dpui) via Admin-PowerShell entfernen
- Anthropic-Account-Email-Änderung bestätigen wenn Support antwortet
