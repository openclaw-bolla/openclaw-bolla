# 2026-04-17 — Opus 4.7 Upgrade + Word-Docs + Mission Control Surface Duo

## Claude-Modell
- **Opus 4.7** freigeschaltet. Mission Control Bolla-Box liest Modellnamen jetzt dynamisch aus der JSONL-Session (`latest msg.model` → `_pretty()` formatiert).
- Remote Trigger „Sonnet-Upgrade-Check" (`trig_01FduNchBR1PE7KM9oXtmh1b`) eingerichtet: Montags 9:00 Berlin prüft Anthropic-Release-Notes, meldet nur bei neuer Sonnet-Version.

## Word-Docs generiert (`/home/bolla/workspace/docs/` + OneDrive-Kopie)
- **`was_ist_bolla.docx`** — Erklärung für Schüler, mit 3 Icons (Gehirn/Tools/Bolla-Pfote). Generator: `scripts/generate_was_ist_bolla.py`
- **`mein_ki_setup.docx`** — technische Rundum-Doku (Claude/Code/Skills/Cloudflare/ADB/MS Graph/Google/Azure/Telegram/GitHub/Cron/Memory/Security) mit 5 PIL-Diagrammen. Generator: `scripts/generate_mein_ki_setup.py`
- **`bolla_recovery.docx`** — Wiederherstellungs-Leitfaden für neuen Rechner, 4 Diagramme. Generator: `scripts/generate_recovery.py`

## Backup-System (Voraussetzung für Recovery-Doc)
- `scripts/weekly_backup.sh` aktiv: sonntags 17:00 via Cron → `/mnt/d/OneDrive/Dokumente/Bolla/backups/`
- Sichert: `workspace/config/`, `~/.cloudflared/`, `crontab -l`, `~/.claude/` (ohne cache/file-history/fremde Projekte), Snapshot-Tarball mit 8-Generationen-Rotation
- rsync mit `--no-perms --no-owner --no-group` für NTFS-Kompatibilität

## Windows-Fix
- PowerToys Peek `EnableSpaceToActivate: true` → `false` (Space in Explorer-Rename öffnete Peek-Dialog). Nach Fix PowerToys-Runner komplett neugestartet.

## Mission Control „Surface Duo" (großer Refactor)
- Nav-Label + Seitentitel: „Surface" → „Surface Duo"
- Jede Box neu: **Aktion + Live-Ergebnis oben**, darunter Trenner „Weitere Befehle" mit 3-4 Beispielen zum Kopieren
- Neue Backend-Endpunkte:
  - `GET /api/adb/info?kind=device|battery|foreground` → Geräteinfo/Akku/aktive App
  - `GET /api/adb/ls?path=...` → Verzeichnislisting mit type/size, toleriert kaputte Symlinks
- Neue Frontend-Buttons: „ℹ Geräteinfo" (Logs-Box), „🔋 Akkustand" (Power-Box), „🔍 Aktive App" (Shell-Box)
- **Apps-Box:** Filter-Input full-width, **live client-side Filtering** nach Laden, Laden-Button als Toggle („✕ Schließen")
- **Screenshot:** auch Toggle
- **Dateien-Box:** Inline-Ordner-Browser via 📁-Button vor Pull/Push-Pfad. Navigation über Klicks, Dateien/Ordner-Auswahl füllt Input. Default-Pfad `/storage/emulated/0/` statt `/sdcard/`

## Gelerntes / Klärungen (für Chris)
- **Chats werden komplett als JSONL** unter `~/.claude/projects/-home-bolla/*.jsonl` aufgezeichnet, Tagebuch ist nur die Essenz am Session-Ende
- Mission-Control-Kurzchats kriegen oft keine Tagebuch-Notiz — Rohdaten aber immer da
- **`/sdcard`** ist Symlink auf `/storage/emulated/0` — **keine** physische SD-Karte, das Surface Duo hat keinen Slot
- **Fotos:** selbst fotografiert → `DCIM/Camera/`, Screenshots + App-Exporte → `Pictures/`
- **Claude Design** ist das neue Anthropic-Produkt für Visual-Creation (seit heute live, auf Max Plan verfügbar, nutzt Opus 4.7)
- Aktuelles bestes freies **Microsoft Phi**: Phi-4 (14B) für Reasoning, Phi-4-mini (3.8B) für kleine HW — via LM Studio verfügbar

## Offen / To-do
- Nichts Akutes. Sonnet-Upgrade-Trigger arbeitet autonom.
