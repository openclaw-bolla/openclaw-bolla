# Session 12.04.2026

## Was gemacht wurde

### Mission Control — Live-Tool-View + Voice
- **Streaming-Mode** für Bolla-Chat: `/api/bolla/chat` läuft jetzt mit `claude -p --output-format stream-json`, Events kommen als NDJSON chunked
- **Tool-Call-Cards**: jeder Bash/Read/Edit/Grep als auffaltbare Card mit Input/Output, Status-Icon ⏳→✓/❌
- **Azure Speech TTS + STT** eingebaut:
  - Neue Endpoints: `/api/bolla/voices` (Liste), `/api/bolla/tts` (MP3)
  - Voice-Control-Bar: Toggle 🔊, Stimmen-Dropdown (150 Stimmen, 21 DE), Lautstärke, Geschwindigkeit (-30% bis +50%), ⏹ Stopp, 🧪 Test
  - Auto-TTS nach Bolla-Antwort
  - **ESC** stoppt Sprachausgabe
  - 🎤-Button mit Web Speech API (de-DE) für Spracheingabe — auto-send wenn Voice-Toggle an
- Initial-Load mit Retry (30× 2s): Mission Control lädt Daten auch wenn es vor der API geöffnet wird
- 10-Min-Auto-Reload für Geburtstage/Robin/Schule/Foto

### Azure Speech Setup
- Resource: `Speech_bolla1` (North Europe erst TextAnalytics erstellt → gelöscht, dann korrekt als SpeechServices neu)
- Config: `/home/bolla/workspace/config/azure_speech.json` (chmod 600)
- Free Tier F0: 500k Zeichen/Monat reichen locker
- Default-Stimme: `de-DE-SeraphinaMultilingualNeural`

### E-Mail-API-Bug gefixt
- `get_emails_outlook()`: Graph-URL-Query hatte unencoded Spaces (`isRead eq false`) → 100% Fehlschläge
- Fix: `isRead%20eq%20false`, analog für `orderby`
- Log spamte nur noch mit "URL can't contain control characters"

### chrismandel.de wp-admin Fehler behoben
- Symptom: `/wp-admin` wirft "ValueError thrown — Missing format specifier at end of string" (PHP sprintf-Warning durch Custom-Error-Handler)
- **SFTP-Zugang** von IONOS genutzt (Host `home31919270.1and1-data.host`, User `p8002904`)
- Übeltäter: **IONOS Must-Use-Plugin** `wp-content/mu-plugins/1and1-subdomain.php` + Plugin `wp-content/plugins/1and1-wordpress-assistant`
- Beide umbenannt auf `.off` → Login funktioniert wieder
- Sicherheitslücken entschärft: `reset_pw.php`, `show_tables.php` im WP-Root → `.off` (von OpenClaw deployed)
- WP-Installation bei IONOS: `clickandbuilds/ChrisPrivat/`

### Windows Autostart aufgeräumt
- HKCU entfernt: **EpicGamesLauncher**, **Adobe Acrobat Synchronizer**, **MicrosoftEdgeAutoLaunch**
- HKLM (Admin nötig, offen): **Nearby Share**, **dpui**
- Backup: `/home/bolla/workspace/backups/autostart_20260412_170715.json`
- Boot-Audit-Script: `/home/bolla/workspace/scripts/boot_audit.sh` (zeigt Autostarts, Tasks, Boot-Zeiten)

### Spotify-Cache geleert
- 817 MB → 227 MB, Leer-Screen-Problem
- Gelöscht: Default/Cache, Default/Code Cache, Default/GPUCache, Default/DawnGraphiteCache, Default/DawnWebGPUCache, GrShaderCache, GraphiteDawnCache
- Login/Playlists bleiben erhalten

## Service-Management
- API läuft aktuell als transient systemd-unit: `bolla-mc-api.service` (systemd-run --user)
- Nach WSL-Reboot übernimmt weiterhin der `@reboot`-Cron-Eintrag
- Wichtig: `claude`-CLI-Pfad wurde via `shutil.which()` robust gemacht (systemd-user hatte kein `/home/bolla/.local/bin` im PATH)

## Notizen zu Chris
- Hat Azure-Keys im Chat geteilt — einmalig OK weil nötig, aber nicht ideal für die Zukunft
- Sein IONOS-Standardpasswort "Brassicaceae13!" — **NICHT gespeichert**, nur für diese Session genutzt
- Schlüssel rotieren sobald er Zeit hat (Azure + SFTP)

## Offene Todos für morgen (13.04.2026)
- **Mission Control remote hosten** via Cloudflare Tunnel
  - Ziel: von zweitem Surface unterwegs auf Heim-API zugreifen
  - Route: `cloudflared` auf Heim-Surface → `bolla.chrismandel.de` → lokale API
  - Auth: Cloudflare Access (kostenlos, Email-Magic-Link oder Google-SSO)
  - Zweiter Tunnel für LM Studio (`lms.chrismandel.de`)
  - Aufwand: 3-4h, erste Hürde: Cloudflare-Nameserver für chrismandel.de (DNS-Propagation 0-24h)
  - IONOS Domain → Cloudflare Nameserver umschalten
  - API-HTML muss API_URL umschaltbar machen (localhost vs. tunnel)
- **HKLM-Autostart** (Nearby Share + dpui) — braucht Admin-PowerShell:
  ```powershell
  Remove-ItemProperty -Path 'HKLM:\Software\Microsoft\Windows\CurrentVersion\Run' -Name 'Nearby Share'
  Remove-ItemProperty -Path 'HKLM:\Software\Microsoft\Windows\CurrentVersion\Run' -Name 'dpui'
  ```

## Chris-Stimmung
- Hat heute viel gleichzeitig angefragt (WP + Azure + Boot + Autostart) — leicht überfordert wenn zu viele Sachen parallel laufen
- Mag klare Schritt-für-Schritt-Anweisungen, besonders bei Azure-UI
- War richtig glücklich als wp-admin wieder lief ("🎉🎉")
- Sprachausgabe fand er "top" — Voice-Feature ist Langzeit-Win
