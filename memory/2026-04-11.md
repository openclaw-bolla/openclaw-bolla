# Session 11.04.2026

## Was gemacht wurde

### E-Mail-Skripte gefixt
- **spam_watcher.py** + **mission_control_api.py** laufen (Cron aktiv)
- API-Server Bug gefixt: URL-Encoding für Outlook Graph API (Leerzeichen in Queries) — Neustart hat gereicht, Code war schon korrekt
- **openclaw-launcher.sh**: Referenz von `outlook_spam_watcher.py` auf `spam_watcher.py` korrigiert
- **outlook_backup.py** in Cron aufgenommen: Sonntags 19:00 automatisch

### Mission Control erweitert
- **Local Server** Seite neu gebaut: Chat mit LM Studio (192.168.178.29:1234, Llama)
- **Screenshot-Funktion** im Bolla-Chat: Strg+V Paste oder 📎 Upload, Bild wird an Claude Code weitergegeben
- Doppelte index.html entdeckt und behoben (Edge lud alte Kopie)

### Workspace-Umzug
- `/home/bolla/.openclaw/workspace/` → `/home/bolla/workspace/`
- Alle Referenzen aktualisiert: Skripte, Crontab, CLAUDE.md, VBS-Autostart
- Symlinks: `.openclaw/workspace` → `~/workspace` (Rückwärtskompatibilität), `~/mission-control` → `~/workspace/mission-control`

### Telegram Bot
- Neues Skript `telegram_bot.py`: lauscht in Familiengruppe, antwortet via Claude Code
- Reagiert auf alle Nachrichten in der Gruppe (nicht nur @-Mentions)
- In Cron aufgenommen (@reboot)
- Config in `telegram_bot.json` ausgelagert (Token nicht mehr hardcoded)
- **Bolla-Avatar** erstellt: Robo-Hund mit Cyan-Augen + Pfote, als Telegram-Profilbild gesetzt

### Windows-Autostart
- VBS aktualisiert: startet jetzt Claude Code im Windows Terminal + Mission Control in Edge
- Pfade auf neuen Workspace korrigiert

### Permissions
- Claude Code Settings: `Bash` pauschal erlaubt, nur rm -rf / und force-push auf main/master geblockt

## Status
- Alle Skripte laufen, API-Server neugestartet, Telegram Bot aktiv
