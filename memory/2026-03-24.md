
## Autostart-Fixes (10:30 Uhr)
- **Problem:** Gateway startete nicht zuverlässig nach Neustart → `openclaw: command not found` im Log
- **Fix:** VBS nutzt jetzt vollständigen Pfad `/home/bolla/.npm-global/bin/openclaw`
- **Problem:** Token Watcher crashte lautlos nach Neustart weil WSL noch nicht bereit
- **Fix:** Token Watcher VBS wartet jetzt 20s (nach WSL-Wakeup) bevor Python startet
- **Mein Fehler:** Debug-Lauf hat Token Watcher ausgelöst → alten Token eingetragen → Chris rausgeschmissen + musste manuell Token neu eintragen. Sorry!
- Token Watcher läuft aktuell (PID 1825), prüft alle 15 Min
- Beim nächsten Neustart sollte alles automatisch klappen

## Portabler Autostart (10:35 Uhr)
- **Hauptproblem erkannt:** Absolute Pfade in VBS (`/home/bolla/...`) → bei Umzug auf neuen Rechner kaputt
- **Lösung:** `openclaw-launcher.sh` als Mittelsmann
  - VBS ruft nur `~/.openclaw/workspace/scripts/openclaw-launcher.sh` auf
  - `~` wird von bash automatisch auf das richtige Home expandiert
  - Script sucht `openclaw` selbst via `which` + typische Fallback-Pfade
  - **Keine hardcodierten Pfade mehr in VBS**
- Bei Umzug auf neuen Rechner: nur WSL-Username darf sich nicht ändern, oder `~` reicht
- Beide VBS-Dateien (Gateway + Token Watcher) aktualisiert
