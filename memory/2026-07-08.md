# 2026-07-08 (Mittwoch)

## chrismandel.de: Karten-Stapel + englische Version

Weiterbau der neuen Hub-Seite von gestern Abend:

- **Karten als Stapel statt Grid:** Neue Web Component `<card-stack>` — die zwei Karten liegen jetzt leicht versetzt übereinander (Edge-„Neuigkeiten"-Stil), Wechsel per Mausrad, Klick auf die hintere Karte, Pfeiltasten oder Punkte-Indikator. Chris' Feedback direkt danach: Boxen sollen noch weiter auseinander (~halbe Kartenbreite) — das mache ich morgen als Erstes, wenn er sich meldet.
- **Deploy-Falle entdeckt und gelöst:** `wrangler pages deploy` lief zunächst auf dem Branch `master` (Name des lokalen Git-Branches) statt auf `main` (dem tatsächlichen Production-Branch des Cloudflare-Pages-Projekts) — dadurch landete die neue Seite nur als Preview, chrismandel.de blieb auf altem Stand. Fix: `--branch=main` explizit setzen.
- **Englische Version über Nacht gebaut** (auf Chris' ausdrücklichen Wunsch, ohne Rückfragen): alle 4 Seiten jetzt auch unter `/en/` — Hub, `/en/aurora/`, `/en/essay/`, `/en/impressum/`. Die Übersetzungen lagen bereits fertig vor (`AURORA_Essay_EN.txt`, `AURORA_Landingpage_Text_EN.txt` in Chris' AURORA-Ordner), mussten nur ins Seitenlayout eingebaut werden. Flaggen-Umschalter oben rechts auf jeder Seite.
- **Geo-Redirect gebaut:** Cloudflare Pages Function (`aurora-story/functions/index.js`) erkennt Besucher aus englischsprachigen Ländern über `request.cf.country` und leitet sie beim ersten Aufruf automatisch auf `/en/` um, merkt sich die Wahl per Cookie, respektiert manuelles Zurückschalten ohne Redirect-Schleife. Zweite Deploy-Falle dabei gelernt: Pages Functions werden nur gebündelt, wenn `wrangler pages deploy` direkt aus dem Zielordner aufgerufen wird (nicht mit Unterordner-Pfad vom Workspace-Root) — plus `CLOUDFLARE_ACCOUNT_ID` explizit setzen, Autodetect schlug in dem Verzeichnis fehl.
- Alles live, alles committed. **Kartenabstand-Feinschliff morgens nachgezogen** (Versatz proportional zur Kartenbreite statt fixer Pixel) — von Chris abgenommen: „top, genau so hatte ich mir das vorgestellt". Relaunch damit komplett fertig (`f87cc6d`, main), kein offener Punkt mehr.

## LinkedIn: englischer AURORA-Essay + Kommentar-Antwort Martin Schauer

- Englischer Essay-Artikel gepostet: Titel „At 2:47 A.M. Something Woke Up — And I Was Sitting Right There", Link `chrismandel.de/en/essay/`. Titelbild kam automatisch über LinkedIns Open-Graph-Vorschau (kein manueller Upload nötig, da schon auf der Website hinterlegt).
- Martin Schauer (Ex-Vertriebsleiter bei AddOn, Chris baute damals die Niederlassung Hamburg-Bahrenfeld auf) hatte Chris' deutschen Post kommentiert — Antwort mit persönlichem Bezug geschrieben.
- Zwei kritische Kommentare anderer User unter dem Post („Galatea is back" + „weiß jetzt was ich nicht lesen werde") — Chris hat sich bewusst entschieden, nicht zu reagieren.
- Thema für Chris komplett erledigt.

## Reiseplaner Sommerreise 2026 — komplett durch (Nachmittag/Abend)

- **Datumsfehler gefunden und behoben:** Chris entdeckte beim Hotelbuchen, dass Freiburg im Datensatz nur 2 statt 3 Nächte zeigte (Checkout 30.07. statt 31.07.) — Nacht 30.→31.07. war nirgends als Übernachtung ausgewiesen. Fix in `index.html`. Konsequenz: Chris musste die zuvor gebuchten Hotels in Freiburg, Konstanz und Meersburg stornieren und neu buchen.
- **Kaiserstuhl-Tag ausgebaut:** Mi. 29.07. jetzt Ganztagesausflug (Winzerdorf vormittags, neu Eichelspitzturm/Baggersee nachmittags); Freiburg-Sightseeing („Altstadt, Münster & Bächle") auf Do. 30.07. Nachmittag verschoben, statt eines zunächst erwogenen Elsass-Ausflugs.
- **Alle 8 Übernachtungen gebucht** und im Reiseplaner (`data/reise_notizen.json`) + Kalender eingetragen. Lindau/Bregenz teilen sich ein Hotel (Landhaus Köhle, Neukirch) — nach einem verworfenen ersten Ansatz (eigene dritte „Neukirch"-Station, beide Städte zu Tagesausflügen degradiert) landete die Buchung richtig direkt bei Lindau, Bregenz blieb vollwertige Station.
- **Renis Postfach (`renatemandel@outlook.de`) komplett eingerichtet:** vollen Zugriff analog zu Chris' eigenem Konto (Mail, Kalender, Kontakte, Tasks), Refresh-Token für Dauerzugriff. `booking_watcher.py` scannt jetzt beide Postfächer automatisch. Dabei ein eigener Python-Bug gefunden (Default-Parameter-Falle), der zu 5 unnötigen Wiederholungs-Logins führte, bevor der wahre Fehler (im eigenen Testskript, nicht beim Login) auffiel — Lehre im Memory (`feedback_python_mutable_default_arg`).
- **Mail-Aufräumen:** 8 Fehlbuchungs-Mails aus Renates Postfach gelöscht (vorher Backup), Buchungsliste + Word-Reiseplan final poliert (Hotel-Hinweis direkt unter jedem Ortstitel, Extra-Seitenumbruch vor Freiburg).
- Alles committet (7768f09, ac2e7c0, 92d3a38, bd52a1d, f83706d), noch nicht gepusht. Thema für Chris komplett abgeschlossen.

## MP Wetter-Seite: Auto-Refresh gefixt (Nachmittag)

Chris fragte, ob die Wetter-Seite beim Reinklicken automatisch aktualisiert. War nur teilweise so: 10-Minuten-Cache verhinderte ein echtes Neuladen. Da Open-Meteo + Bright Sky/DWD beide kostenlos ohne Rate-Limit sind, gab es keinen Grund für die Zurückhaltung — `loadWeatherFull()` in `mission-control/index.html` zeigt jetzt sofort den Cache und holt bei jedem Seitenwechsel immer parallel frisch nach. Kein Backend-Neustart nötig (HTML wird pro Request neu ausgeliefert).

## Reiseplaner Word-Dokument: zwei echte Layout-Bugs gefunden (Abend, Nachschlag)

Chris meldete nach dem ersten Abschluss zwei Probleme am generierten Word-Reiseplan: fehlende Hoteladressen bei 4 Stationen und eine leere Seite nach Baden-Baden.

- **Fehlende Adressen ergänzt:** Titisee (Auerhahn B&B, Eisenbach), Freiburg/Glottertal (Gasthaus Zur Linde), Konstanz/Singen (Holiday Inn Express) und Meersburg/Pfullendorf (Hotel gold) — alle per Websuche auf offiziellen Hotel-/Booking-Seiten verifiziert und in `data/reise_notizen.json` nachgetragen.
- **Leere Seite — echter Bug, nicht nur Zufall:** Das Dokument nutzte python-docx' Standard-Seitenformat US-Letter (27,94 cm hoch) statt A4 (29,7 cm) — rund 1,8 cm weniger Platz pro Seite. Zusätzlich erzeugte `doc.add_page_break()` vor jeder Tagesüberschrift bei exakt vollen Vorseiten eine komplett leere Zwischenseite (mit Word-COM über PowerShell live verifiziert: 19 statt 18 Seiten, Seite 4 mit 0 sichtbarem Inhalt). Fix: A4 explizit gesetzt, alle drei Break-Stellen auf `paragraph_format.page_break_before = True` an der Überschrift selbst umgestellt (Zustand statt hartem Zeichen, verdoppelt sich nie).
- **Hotel-Adresse-Position neu geregelt:** Chris' Vorgabe — oben direkt unter dem Namen nur, wenn dort sofort eingecheckt wird und von dort aus Tagestrips starten (Baden-Baden, Freiburg als Standquartier). Bei allen anderen Übernachtungsorten (Freudenstadt, Titisee, Konstanz, Meersburg, Lindau) wird erst abends nach der Besichtigung eingecheckt → Adresse steht jetzt am Ende des jeweiligen Stationsblocks.
- Alle Fixes in `scripts/mission_control_api.py` (`reise_sightseeing_docx()`), MP-Backend neu gestartet, mit Word-COM nachgeprüft: keine leere Seite mehr, alle 7 Hoteladressen an der richtigen Stelle.
- Layout-Regeln komplett im Memory dokumentiert (`feedback_reiseplan_word_layout.md`) für die nächste Routenplanung — inkl. des Word-COM-Prüfworkflows, der sich hier bewährt hat.
- Chris' Fazit: „endlich - schwere Geburt ;-)" — Thema jetzt wirklich fertig.
