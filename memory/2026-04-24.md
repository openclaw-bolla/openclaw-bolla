# Tagesnotizen 2026-04-24

## Surface Pro SSH-Einrichtung — ERLEDIGT ✓

**Ziel:** SSH + Reverse-Tunnel wie beim Surface Book (Port 2223)

**Erledigt:**
- IP: `192.168.178.41`, Windows-User: `renat` (Renate Mandel, Reni)
- OpenSSH Server installiert und läuft
- Firewall-Regel Port 22 gesetzt
- SSH-Key in `C:\Users\renat\.ssh\authorized_keys` eingetragen + icacls-Rechte gesetzt
- Key nach `C:\ProgramData\ssh\administrators_authorized_keys` kopiert (Admin-Konto-Fix)
- Tunnel-Skript `surface_pro_tunnel.ps1` auf Port 2223 eingerichtet
- Mission Control API: `get_pro_health()` + `do_pro_action()` + Routen `/api/surfaces/pro` + `/api/surfaces/pro/action`
- Mission Control HTML: Surface Pro Block + `loadProHealth()` + Brain-Graph-Knoten
- Memory updaten: user_devices.md

**Sonstiges:**
- Reni = Renate Mandel, Chris' Frau (Abkürzung merken)
