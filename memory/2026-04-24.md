# Tagesnotizen 2026-04-24

## Surface Pro SSH-Einrichtung (in Arbeit)

**Ziel:** SSH + Reverse-Tunnel wie beim Surface Book (Port 2223)

**Erledigt:**
- IP: `192.168.178.41`, Windows-User: `renat` (Renate Mandel, Reni)
- OpenSSH Server installiert und läuft
- Firewall-Regel Port 22 gesetzt
- SSH-Key in `C:\Users\renat\.ssh\authorized_keys` eingetragen + icacls-Rechte gesetzt

**Problem:** Key-Auth schlägt noch fehl
- Wahrscheinliche Ursache: `renat` ist Administrator-Konto
- Fix: Key zusätzlich nach `C:\ProgramData\ssh\administrators_authorized_keys` kopieren
  ```powershell
  Copy-Item C:\Users\renat\.ssh\authorized_keys C:\ProgramData\ssh\administrators_authorized_keys
  icacls C:\ProgramData\ssh\administrators_authorized_keys /inheritance:r /grant "SYSTEM:F" /grant "BUILTIN\Administrators:F"
  ```

**Nächste Schritte nach erfolgreichem SSH-Login:**
1. Tunnel-Skript `surface_pro_tunnel.ps1` erstellen (Port 2223, analog zum Book)
2. Als Windows Task Scheduler Job einrichten (autostart bei Login)
3. Mission Control API + HTML updaten (analog Book, neue Sektion "Surface Pro")
4. Memory + MEMORY.md updaten

**Sonstiges:**
- Reni = Renate Mandel, Chris' Frau (Abkürzung merken)
- Quick Assist als Alternative zum Abtippen angeboten
