# 2026-09-03 (Mi→Do)

Session vormittags, Chris zwischendurch am Book, ging danach in die Schule und cleart anschließend.

## DistroKid-Aufräumen „different buttons same heartbeat"
- Song bei DistroKid eingereicht + Social-Media-Paket fertig (Chris).
- Desktop-Staging-Ordner + lose `.wav` (31 MB) → `backups/distrokid_desktop_archiv/…_20260903_103021/` (soft).
  `Desktop/DistroKid/` jetzt leer. Master-Archiv behält finale mp3 + Cover.
- 13 Cover-Test-Kandidaten vom 02.09. (`*_cover_1/2/3.jpg` ohne mp3) → `Suno_DistroKid/_aussortiert/`.
- Song als Nr. 8 / done in `config/bollawave_release_schedule.json`.

## 5-Stunden-Limit
- Zwei Klärungsrunden. Ergebnis:
  1. Vor größeren Projekten beide Limits checken, **< 10 % Rest = nicht starten** → harte Schwelle
     in `feedback_grossprojekt_budget_check`.
  2. **Notfall-Hook** in `~/.claude/hooks/context_check.py`: meldet bei jedem Prompt, sobald das
     5h-Fenster **< 10 % Rest** hat (`five_hour_pct` > 90) → Block „🚨 5h-RESERVE FAST WEG".
     Grund: Chris will am Book in der Schule nicht plötzlich ohne Bolla dastehen.

## MP-Überblick Kosten (`config/kosten.json`)
- RouteNote raus (Konto tot), **DistroKid rein** (Musician-Plan ~23,99 €/Jahr).
- Gemini-/MAI-/Suno-Info-Texte entstaubt.
- **Self-Metering geschärft:** `_suno_render_cover_bytes()` zählt jetzt jedes Cover mit
  (`kosten_ledger_add`). Neu `scripts/kosten_add.py` für Terminal-MAI-Jobs
  (`kosten_add.py mai <n> "Notiz"`, `--list`). Ledger-Altkey `mai-image-2.5` → `mai_image`.
- Billing per API geht nicht (nur Modell-Keys, kein Cost-Zugriff) — daher self-metered Schätzung.

## Ausflugsplan Lüneburger Heide
- **Bester Tag: Sonntag 06.09.** (Fr Dauerregen, Sa grau/kühl; So trocken, sonnig, windstill —
  Open-Meteo).
- **Laufprofil aktualisiert:** Renis Bein besser, ebene Spaziergänge bis ~8–10 km tun der Arthrose
  gut. `user_laufprobleme` überarbeitet.
- Plan v2: **Wanderung Undeloh → Radenbachtal → Wilsede (Milchhalle) → Totengrund → zurück**,
  ~9–10 km flach. Wilseder Berg optional. Kaffee danach in Undeloh. ID.3 ~100 km, kein Ladestopp.
- **Feedback Chris:** „Heideblüte im Ausklang" war geschätzt — **offizielles Barometer: 100 %**
  (02.09.), Wilseder Berg ~90 %, Totengrund ~70 %. Word mit Quelle korrigiert.
  Neue Regel `feedback_saisonale_zustaende_nachschauen`: Blüte/Schnee/Pegel nie schätzen.
- Datei: `D:\OneDrive\Desktop\temp\Ausflug Lueneburger Heide - So 06.09.2026.docx` (bleibt bis
  nach dem Ausflug bzw. Chris' OK).

## Commits (workspace)
- DistroKid-Aufräumen + Schedule
- aktuell.md Limit-Regel-Korrekturen
- Kosten-Self-Metering + kosten_add.py
- MP-Überblick Kosten-Kacheln
