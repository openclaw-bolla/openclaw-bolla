# 2026-05-27 — Session-Notiz

## Surface Book Lüfter/CPU (100% → 1%) ✅

Lange Debugging-Session. Täter in Reihenfolge:
1. **msedgewebview2 (Windows Widgets)** — 472% CPU! → Policy gesetzt + WebExperience Pack entfernt (dauerhaft)
2. **Windows Search (WSearch/SearchIndexer)** — immer wieder bei 70–80% → dauerhaft deaktiviert
3. **Windows Update (wuauserv)** — 102% CPU → mehrfach gestoppt, startet sich selbst neu (normal)
4. **Edge Renderer-Prozesse** — einzelne bei 146–205s akkumulierter CPU-Zeit → wiederholt gekillt
5. **Spotify (7 Proz.), Adobe CC (8 Proz.), WhatsApp.Root** → gekillt

Abends läuft ein Windows Preview-Update — danach sollte Dauerbetrieb ruhig sein.

## Surface Book Boot ✅

HiberbootEnabled permanent via Group Policy + SYSTEM-Task "BollaKeepHiberbootOff" gesetzt. Boot funktioniert seitdem zuverlässig.

## Mic Web Speech API — noch offen

- speech.googleapis.com erreichbar (TCP 443: True)
- Windows Privacy = erlaubt
- Win+H läuft jetzt zuverlässig
- Mic in MC weiter inkonsistent → letzter Versuch: Mic-Pegel auf 85–90% erhöhen
- Studio: WISO Internet Security läuft noch, muss Chris manuell deinstallieren

## Edge Tab-Gruppen

Verloren gegangen durch Force-Kill der Edge-Prozesse (mea culpa).  
Fix: Edge → Startup → "Registerkarten aus vorheriger Sitzung öffnen" aktivieren.

## Nächste Session

1. Mic-Pegel auf Book prüfen/erhöhen (85–90%) → Web Speech API nochmal testen
2. WISO auf Studio deinstallieren → Speech API testen
3. Prüfen ob Windows Update Preview-Update erfolgreich war
