# 13.09.2026

- **git-push-Hänger endlich gelöst — war nie ein Netzwerkproblem:** Nach dem `wsl --shutdown` erneut getestet, hing weiter trotz allem bisherigen Aufwand. Systematisch durchprobiert: Checksum-Offload auf Windows-Adapter (physisch + vEthernet-WSL) deaktiviert, `http.version HTTP/1.1` erzwungen, Large Send Offload v2 + Receive Segment Coalescing auf dem vEthernet-WSL-Adapter aus, TSO/GSO/GRO auf WSL-eigenem `eth0` aus, WISO Internet Security (F-Secure-Engine) testweise 15 Min. pausiert — alles ergebnislos, jede Einzelmaßnahme fühlte sich nach dem richtigen Verdächtigen an, war es aber nicht.
- **Echte Ursache:** Direkter curl-Test gegen den echten GitHub-Endpunkt (chunked POST, mit echten Zugangsdaten) antwortete in 0,35s — kein Netzwerkfehler. Der `/proc/<pid>/io`-Zähler des laufenden `pack-objects`-Prozesses zeigte dann den entscheidenden Hinweis: 500MB+ bereits in die Pipe geschrieben, für einen angeblich 9KB kleinen Push. Ursache: Die automatische Nachtsicherung vom 09.09.2026 hatte trotz vorhandener `.gitignore`-Regel zwei riesige Backup-tar-Dateien (Robin-Tansania-Reorg 1,6GB + Kilimanjaro-WhatsApp-Backup 212MB) versehentlich in einen Commit gezogen. Jeder Push seither versuchte, ~1,8GB hochzuladen — sah bei kurzen Testtimeouts wie ein Hänger aus.
- **Fix:** Interactive Rebase auf den betroffenen (noch ungepushten) Commit, beide Dateien per `git rm --cached` aus der Historie entfernt (liegen unverändert auf der Platte weiter). Push danach: 9KB, 4 Sekunden. `scripts/nacht_backup.sh` gehärtet — `git add` schließt `backups/*.tar` jetzt explizit per Pathspec aus, plus generisches Sicherheitsnetz verwirft automatisch jede gestagte Datei >20MB vor dem Commit. Alle Windows/WSL-Netzwerk-Einstellungen wieder auf Standard zurückgesetzt, da nachweislich nicht die Ursache. 18+ Commits erfolgreich auf GitHub gepusht.
- **Lehre für künftige ähnliche Fälle:** Vor jeder Netzwerk-Ferndiagnose bei „git push hängt" zuerst `git rev-list origin/master..HEAD --objects | git cat-file --batch-check='%(objecttype) %(objectsize) %(rest)' | sort -k2 -nr | head` prüfen — zeigt sofort, ob ein Riesen-Blob im Spiel ist. Details: [[project_git_push_wsl_chunked_hang]].
- **AURORA II — Satz-Feedback von Telegram/Cron auf synchron im MP umgebaut:** Chris fand Kapitel 1 schon sehr gelungen (Humor/Spannung nah am Optimum), wollte aber den Weg über Telegram für einzelne Satz-Rückfragen loswerden. Neuer Ablauf direkt in MP: Satz markieren → Button „🚩 Frage/Korrektur zu markiertem Satz" → Bemerkung/Frage/Korrekturwunsch eingeben → sofortige Antwort (Erklärung und/oder Verbesserungsvorschlag) → Chris klickt „✅ Übernehmen" oder „❌ Verwerfen" → bei Übernehmen sofortiger Einbau, keine Wartezeit. Alter Cron `aurora2_satz_korrektur.py` (alle 3 Min., Telegram-Bescheid) komplett entfernt (Skript gelöscht, Cron-Eintrag raus) — Logik jetzt inline in `mission_control_api.py` als Job-Pattern (`/api/aurora2/satz-frage` + `-status` + `/api/aurora2/satz-entscheidung`, analog zum bestehenden `/api/aurora2/generiere`-Muster). Chris hat das Tool noch während der Session selbst am Kapitel-1-Auftakt ausprobiert (Vorschlag zu Recht abgelehnt) — funktioniert im echten Einsatz.
- **AURORA II — fester Opus-Modus für alle Buchinhalte:** Auf Chris' Nachfrage entschieden: `/api/aurora2/generiere` (Kapitel schreiben) UND das neue `/api/aurora2/satz-frage` laufen jetzt beide auf `claude-opus-5` statt Sonnet — Wochenbudget war bei nur 10% (Reset erst Sonntag), also unkritisch. Buch I (`ki_buch.json`, fertig/live) bleibt bewusst außen vor. Details: [[feedback_modell_auswahl]].
- **AURORA II — Vorwort geschrieben (Kombi aus allgemeinem Vorwort + optionalem „Was bisher geschah"):** Chris' Frage: Kapitel 1 setzt Buch-I-Wissen voraus (Theos Tod, Roths Verrat, Maria Santos' Briefe) — Neu-Leser könnten die emotionale Wucht verpassen ("Theo ist tot — ja und?"). Diskutiert und entschieden: keine Änderung an Kapitel 1 selbst (die Andeutungen sind bewusste, gute Technik), stattdessen ein Vorwort davor, analog zu Buch I aber eigenständig. Mit Opus geschrieben (Kontext: Buch-I-Vorwort als Stilvorbild, Charakterbios, Kapitel-1-Volltext), zwei Teile: allgemeines Autoren-Vorwort (Fortsetzung des Themas Mensch-KI-Kollaboration) + klar als überspringbar markiertes „Was bisher geschah" — bewusst OHNE Theos Tod zu verraten, damit Kapitel 1 den emotionalen Schlag noch liefern kann. Liegt jetzt in `aurora2.json` → `vorwort`, in MP über neuen Menüpunkt „📖 Vorwort" sichtbar UND mit dem neuen Satz-Tool bearbeitbar (Sentinel `kapitel_titel: "__VORWORT__"` im Backend). Noch offen: Chris' Feedback zum Text selbst.
- **AURORA II — Lesemarke-Bug nach Satz-Übernahme gefunden & gefixt:** Chris hatte in Kapitel 1 einen markierten Satz korrigieren lassen und übernommen — Korrektur kam korrekt im Text an, aber die rosa „Lesemarke" (zeigt an, bis wohin gelesen/korrigiert wurde) verschwand. Ursache: Die Lesemarke wird beim Übernehmen einer Korrektur nicht mitgezogen, sondern zeigt weiter auf den alten Satztext — der nach dem Ersetzen im Kapiteltext nicht mehr vorkommt, wodurch die Highlight-Suche (`text.includes(satz)`) leerläuft und stillschweigend auf unmarkierten Text zurückfällt. Fix in `mission_control_api.py` (`/api/aurora2/satz-entscheidung`, Zweig „annehmen"): setzt `lesemarken[kapitel_titel]` jetzt auf den neuen Satz statt den alten stehen zu lassen. Zusätzlich Frontend-Fix in `index.html` (`aurora2SatzEntscheidung`): patcht nach dem Übernehmen jetzt auch `_aurora2Data.lesemarken` lokal und rendert über `aurora2RenderInhaltMitLesemarke` statt nur plain `textContent`, damit die Markierung ohne Reload sofort sichtbar ist. Bestehender verwaister Lesemarke-Eintrag für Kapitel 1 nachträglich auf den neuen Satz korrigiert. MC-Server neu gestartet (PID 40954), Fix ist scharf.
