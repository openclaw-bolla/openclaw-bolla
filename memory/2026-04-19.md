# 2026-04-19 — HP-Drucker + LM Studio Setup + Mobile PWA "MC Chris"

## HP Envy Inspire 7900e
- Fehlermeldung "Fotofach blockiert" + laute Geräusche. Troubleshooting durchgegangen: Hauptfach ziehen (leicht anheben, Ausgabeablage vorher einschieben), Fotofach rausnehmen, hintere Duplex-Klappe, Schlitten prüfen.
- Ersatzteil bei Bedarf: Paper Feeder Input **1W2Y8-90004** (enthält Haupt- + Fotofach) — eBay oder HP Parts Store Deutschland.

## LM Studio / Phi-4-reasoning-plus
- Context in LM Studio erhöhen: Modell entladen, im Load-Panel **Context Length**-Slider + **Flash Attention** aktivieren.
- Hardware-Check: Surface Laptop Studio 2, i7-13700H, 16 GB RAM, RTX 4050 **6 GB** VRAM. Empfehlung für phi-4-reasoning-plus Q4_K_M (9 GB): Context ≤ 8192, GPU-Offload ~15–18 Layer, Flash Attention AN.
- Aufgeklärt: **phi-4-reasoning-plus denkt absichtlich lange** (500–2000 Denk-Tokens vor Antwort). Für Smalltalk besser phi-3.1-mini oder phi-4 (ohne reasoning).
- LM Studio Modell-Pfad: **`D:\KI Files\Models`** (Chris hat das von Default auf D: umgebogen, sinnvoll wegen C-Platz).

## Mission Control — Bugfixes
- **LM-Studio-Proxy Timeout** in `mission_control_api.py` Zeile 1006 von `timeout=5` auf `timeout=300` erhöht. Vorher Timeouts bei jedem Reasoning-Chat.
- **Local-Server Tab UI:** Überschrift + Sublabels waren auf weißem Overlay unlesbar. Farben angepasst:
  - Hauptüberschrift: `#0c1420` + `font-weight: 600`
  - Sublabels (Modell, Status, Endpoint-Footer): `#3a5068`

## Mobile PWA "MC Chris" (neu)
- **Zweck:** Mobile-optimierte Mission-Control-Ansicht für Surface Duo (Chris) + iPhone (Robin), installierbar als PWA.
- **Neue Dateien** in `/home/bolla/workspace/mission-control/`:
  - `mobile.html` — iOS-inspiriertes Design (helle Cards, Bottom-Nav, 4 Tabs: Kalender, Mails, Wetter, Geburtstage)
  - `manifest.webmanifest` — PWA-Manifest (scope `/m`, theme `#007aff`)
  - `sw.js` — Network-first Service Worker mit Offline-Fallback
  - `mc-icon-192.png`, `mc-icon-512.png`, `mc-apple-touch.png` — via PIL generiert (blauer iOS-Kachel-Style mit "MC")
- **Server-Routen** in `mission_control_api.py` in `do_GET`: `/m`, `/mobile.html`, `/manifest.webmanifest`, `/sw.js`, `/mc-icon-*.png`
- Bewusst rausgelassen: Bolla Chat + Local Server (Chris will mobile nur Info-Tabs).

## Cloudflare Access
- Access-Policy "Chris" (ALLOW, von 2 Anwendungen genutzt: LM Studio + Bolla Mission Control) erweitert: zweite Einschließen-Regel mit `robinmandel@outlook.de` hinzugefügt.
- Klickpfad im deutschen Cloudflare-Dashboard: Zero Trust → Zugriff → Richtlinien → "Chris" → Konfigurieren → "Einschließen hinzufügen".

## Mail an Robin
- HTML-Mail an `robinmandel@outlook.de` mit Installations-Anleitung für MC Chris auf iPhone (Safari → Teilen → "Zum Home-Bildschirm"). Versendet via Graph API `/me/sendMail`, HTTP 202.

## Erkenntnisse / Klärungen
- **Robin** fragt im Namen auf/anbinden = Cloudflare Access Policy um E-Mail erweitern, dann Magic-Link-Login auf Empfänger-Seite.
- **Deutscher Cloudflare:** "Access" → "Zugriff", "Applications" → "Anwendungen", "Policies" → "Richtlinien", "Include" → "Einschließen".
- Cloudflare UI hat sich 2026 geändert: Policies sind jetzt zentral wiederverwendbar unter "Zugriff → Richtlinien" (nicht mehr nur via App-Editor).

## Memory-Updates
- Neu: `feedback_lokale_modelle.md` — keine Produktiv-Use-Cases für lokale Modelle pushen (Chris nutzt Claude/Copilot)
- Neu: `feedback_sprache_websites.md` — Chris nutzt deutsche UI-Version überall, Klick-Anleitungen auf Deutsch

## Session 2 (Abend)

### Modell-Vereinbarung
- Default-Modell auf **Sonnet 4.6** gesetzt (`settings.json`: `"model": "claude-sonnet-4-6"`)
- Opus nur auf Anfrage — Bolla schlägt Opus vor wenn Aufgabe es rechtfertigt, Chris schaltet via `/model`
- Bolla kann Modell nicht selbst zur Laufzeit wechseln

### SOUL.md Watcher
- `soul_watcher.py` erstellt in `/home/bolla/workspace/scripts/`
- Cron alle 2 min: loggt mtime-Änderungen + ps-Snapshot nach `soul_watcher.log`
- Baseline gesetzt: mtime 1776525945.5

### Telegram Bot Fix
- Bug: `handle_adb_command()` hat bei **jeder** Nachricht sofort die ADB-Fehlermeldung geschickt (nicht erst nach Keyword-Check)
- Fix: erst Keywords prüfen, dann Connected-Check; `adb connect` geht ohne bestehende Verbindung direkt durch
- Bot neu gestartet (PID 229891)
- Entschieden: kein ADB-Auto-Discovery via mDNS — nicht nötig

## Offen / To-do
- Mobile PWA-Erweiterung bei Bedarf (Fotos, Robin-Info, System-Status light, Mail-Push) — wenn Chris konkret was wünscht.
- QR-Code-Suche in Recap-PPTs abgebrochen (Zahnräder statt QRs gefunden); Chris geht über alte OneDrive-Versionen selbst.
