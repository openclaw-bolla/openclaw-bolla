# 2026-06-16

## MC „Failed to fetch" gejagt & gefixt
- **Symptom:** Bolla-Kachel in Mission Control (lokal, Studio) warf sporadisch „Failed to fetch".
- **Diagnose:** ZWEI `mission_control_api.py`-Prozesse lauschten gleichzeitig auf Port 18790 (`SO_REUSEPORT`). Reboot-Race: `@reboot`-Start + `*/2`-Watchdog feuerten zeitgleich (beide PIDs 17:56:01), Watchdog (`pgrep|head -1`) bemerkte den Doppelstart nie. Kernel-Round-Robin → ~50 % der Requests beim hakeligen Prozess → sporadischer Abbruch. Log bestätigte den Race (Watchdog-Eintrag 17:56:01).
- **Fix Wurzel:** `SO_REUSEPORT` aus `mission_control_api.py` entfernt (nur noch `SO_REUSEADDR`). Zweitstart scheitert jetzt sauber mit „Address already in use".
- **Fix Defense-in-Depth:** `mc_watchdog.sh` erweitert um Doppelstart-Guard (echten Port-Inhaber via `ss` behalten, überzählige killen; kein Listener → kill+restart). Live mit Fake-Doppelgänger getestet: greift korrekt.
- **Verifiziert:** 1 Prozess, HTTP 200, Bolla-Chat end-to-end ok.

## AURORA — Verkaufsreife-Check
- **Wort-Check beide Sprachen** (~220k Wörter): DE 1 Fehler `einGeständnis` (Kap22) → „ein Geständnis" gefixt, DE-EPUB neu gebaut + verifiziert. EN 0 Fehler. Sehr sauber.
- **Inhaltl. Einschätzung** (Basis Lektoratsbericht 14.06 + Status „Welle 1+2 abgeschlossen", `buch_fertig: True`): Spannung stark (kein Hook <8), Twist-Mechanik sauber gepflanzt, keine Plotlöcher, Ende löst auf. Die 2 „offenen Köder" sind real keine Löcher. → **verkaufsreif.**
- **Frischer Gegencheck** als Workflow gestartet (wf_3462dc13-b5d, 47 Kapitel-Leser + Opus-Synthese), prüft ob Welle 1+2 gelandet. Report → `/mnt/d/OneDrive/Dokumente/AURORA/AURORA_Gegencheck_16-06.md`. Lief beim Session-Sichern noch.
- **Korrektorat-Tool (LanguageTool):** für diesen KI-Text als unnötig eingestuft (dass/das macht KI eh richtig; Klebungen schon manuell gecheckt).

## Status Ende
Vor `/clear` gesichert (Git + OneDrive). Gegencheck-Report kommt als Datei, in neuer Session aus Datei vorlesen.
