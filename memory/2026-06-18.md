# Tagesnotiz — 2026-06-18 (Donnerstag)

## Kontakte-Aufräumung (Runde 2) — KOMPLETT
- Outlook = Gmail = 152 Kontakte, deckungsgleich, Sync 0 Fehler. Nummern auf wählbares E.164 + Outlook-Gliederung, Anreden raus, `fileAs` „Nachname, Vorname".
- Sync-Matching-Bug gefixt (E-Mail → Mobilnummer → Name, nur Mobile). Gmail-Altdubletten bereinigt.
- ⚠️ Sync-Lösch-Falle: beim Löschen IMMER beidseitig (`delete_contacts_both.py`).

## AURORA Distribution
- **D2D:** DE + EN beide fertig eingereicht („Ebook Success!"), alle Stores außer Amazon, je 4,99 €/$4.99.
- **KDP:** DE + EN fertige Entwürfe, warten NUR auf Amazons Bankprüfung (bis 3 Tage). Danach manuell „Veröffentlichen" klicken. KI-Angabe auf JA.

## Abend-Session — drei Wächter/Hook-Fixes + RouteNote
- 🐛 **Shop-Wächter Amazon-Fehlalarm:** `aurora_shop_watcher.py` meldete fälschlich „AURORA live bei Amazon" — Amazon-Suchseite spiegelt die Query im HTML → `_scrape_has` immer True. Fix: Amazon-Check raus (= KDP-Weg), NO_RESULTS-Filter gegen Query-Reflexion, UBL-Text ehrlicher, State zurückgesetzt.
- 🐛 **Kontext-Warnung-Hook:** `context_check.py` zählte einen globalen, sessionübergreifenden Counter (`/tmp/bolla_msg_count`, nie zurückgesetzt) → meldete „435 Nachrichten" in 10-Min-Session (5× Fehlalarm). Fix: echte Kontextgröße in Tokens aus dem Transkript (stdin `transcript_path`), 1× pro Session ab ~150k. Memory [[feedback-kontext-warnung-hook]].
- 📬 **RouteNote:** 40 Titel >1 Monat in Review. Recherche: aktuell 32–34 Werktage offiziell (hoher Andrang) → NORMAL. Support-Mail mit 2 Fragen gesendet (Review-Dauer + Suno/KI-Deklaration, kein Feld beim Upload gefunden). Auto-Reply kam (nur Eingangsbestätigung). `kdp_mail_watcher.py` um RouteNote-Absender + Auto-Reply-Filter erweitert → echte Antwort kommt per Telegram.
- 🤖 **KI-Transparenz** als Chris' Grundhaltung festgehalten: KI = Werkzeug wie Taschenrechner, immer angeben wo möglich (TikTok, KDP, Suno), nie verschweigen. Memory [[user-ki-transparenz]].
