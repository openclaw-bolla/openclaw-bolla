# Secrets - niemals committen!
config/ms_token.json
config/token_watcher_state.json

# Logs
logs/

# Python
__pycache__/
*.pyc
config/google_client.json
config/google_token.json
config/wtnet_account.json
config/telegram_bot.json
config/azure_speech.json
config/gemini_api.json
config/outlook_oauth2.json
config/outlook_token.json
config/gemini_api_free.json
tools/jadx.zip
tools/jadx/lib/*.jar
tools/*.apk
config/google_gmail_drive_token.json
config/

# Browser-Automatisierung (Playwright-Libs ohne sudo, binär, nicht versionieren)
scripts/browser_libs/
.secrets/

# Fotoseite /f — Laufzeit-Zustand (server-seitig regeneriert)
mission-control/f-photos/served.json
scripts/build_f.log

# Backups (Tar-Archive vor Foto-Umsortierungen etc.) — oft >100MB, sprengt GitHub-Limit
backups/*.tar

# Scratch ist per Definition Wegwerfkram (Claude-Scratchpad-Konvention) — nie committen.
# Vorfall 18.09.2026: Nachtsicherung hat ~500MB entpackte pptx-Testdateien aus scratch/
# committet+gepusht (.git auf 2,9GB aufgebläht), weil jede Einzeldatei unter dem
# 20MB-Limit aus nacht_backup.sh lag — der Schutz greift nur pro Datei, nicht aggregiert.
scratch/
